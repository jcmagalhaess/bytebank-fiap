version: "3.8"

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    ports:
      - "3333:3333"
    command: sh -c "npx prisma generate && npx prisma migrate deploy && npm run dev"
    volumes:
      - ./api:/app
      - /app/node_modules
    env_file:
      - ./api/.env
    restart: always
    depends_on:
      - db
    networks:
      - bytebank-net

  angular-host:
    build:
      context: ./frontend/angular-host
      dockerfile: Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    ports:
      - "4200:4200"
    command: npm start
    restart: always
    volumes:
      - ./frontend/angular-host:/app
      - /app/node_modules
    depends_on:
      - api
    networks:
      - bytebank-net

  angular-remote:
    build:
      context: ./frontend/angular-remote
      dockerfile: Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    ports:
      - "4201:4201"
    command: npm start
    restart: always
    volumes:
      - ./frontend/angular-remote:/app
      - /app/node_modules
    depends_on:
      - api
    networks:
      - bytebank-net

  db:
    image: postgres:14
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bytebank
      POSTGRES_PASSWORD: bytebank-2025
      POSTGRES_DB: bytebank_db
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - bytebank-net

volumes:
  pgdata:

networks:
  bytebank-net:
    driver: bridge
