/** @type {import('next').NextConfig} */
const nextConfig = {
  // Adicione esta linha para habilitar o output standalone
  output: "standalone",
  reactStrictMode: true,
};

module.exports = nextConfig;
